services:
  app:
    build:
      context: .
      additional_contexts:
        dataharmonizer-src: ../DataHarmonizer
        linkml-lib: ../linkml-lib
    environment:
      DATAHARMONIZER_DIR: /DataHarmonizer
    ports:
      - "8765:8765"
    volumes:
      - ../DataHarmonizer:/DataHarmonizer:ro
